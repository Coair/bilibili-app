# B站下载工具 - 使用指南（零基础版）

> 本指南写给 **完全没有编程经验** 的用户，每一步都有截图式的文字说明。跟着做就能用。

---

## 一、这个工具能干什么？

- 输入 B站视频链接 → 下载高清视频（MP4格式）
- 也可以只下载音频（MP3格式），相当于一个免费的音乐下载器
- 支持一次粘贴多个链接，批量下载

---

## 二、你需要准备什么？

### 2.1 安装 Python（只需做一次）

1. 打开浏览器，访问 **https://www.python.org/downloads/**
2. 页面中间有一个黄色大按钮，写着 `Download Python 3.x.x`，点击下载
3. 下载完成后，双击打开安装文件
4. **⚠️ 重要！** 在安装界面的最底部，勾选 **「Add Python to PATH」**（把 Python 加到系统路径）
5. 然后点击 `Install Now`，等待安装完成
6. 安装完成后，**重启电脑**

> 验证是否安装成功：按键盘 `Win + R`，输入 `cmd` 回车，在黑色窗口里输入 `python --version` 回车，如果出现 `Python 3.x.x` 就说明装好了。

### 2.2 下载这个项目

**方式一：下载 ZIP 压缩包（推荐，最简单）**

1. 打开浏览器，访问：
   ```
   https://github.com/Coair/bilibili-app
   ```
2. 页面右上角有一个绿色按钮 **「<> Code」**，点击它
3. 在弹出的菜单里选择 **「Download ZIP」**
4. 下载完成后，**右键点击 ZIP 文件 → 选择「解压到当前文件夹」**
5. 把解压出来的 `bilibili-app` 文件夹放到你喜欢的位置
6. 建议放在 **D盘** 或 **桌面**，路径里最好不要有中文

**方式二：通过 Git 下载（适合有 Git 的用户）**

```bash
git clone https://github.com/Coair/bilibili-app.git
```

---

## 三、一键启动（推荐！）

文件夹里有一个 `start.bat` 文件，**双击它** 就能自动完成一切：

1. 自动检查 Python 是否安装
2. 自动安装缺少的依赖
3. 自动打开浏览器，进入下载页面

> 就是这么简单！第一次双击时会自动装依赖，可能需要一两分钟。以后每次双击就直接启动了。

---

### 备用方式：手动命令启动（如果 bat 文件不能用的备选方案）

<details>
<summary>点击展开手动启动步骤</summary>

#### 3.1 打开命令窗口

1. 打开 `bilibili-app` 文件夹
2. 在文件夹的 **空白处**，按住键盘 `Shift` 键不放，然后点击鼠标右键
3. 在弹出的菜单里选择 **「在此处打开 PowerShell 窗口」**（Win10）或 **「在终端中打开」**（Win11）

#### 3.2 安装依赖

在命令窗口中输入，然后按回车：

```
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
```

> 如果安装失败，可以去掉 `-i https://pypi.tuna.tsinghua.edu.cn/simple` 再试一次

#### 3.3 启动程序

接着输入：

```
python app.py
```

按回车即可。

</details>

> ⚠️ 这个命令窗口 **不要关掉**！关掉程序就停了。

### 3.4 打开使用界面

打开浏览器（推荐 Chrome 或 Edge），在地址栏输入：

```
http://localhost:5001
```

按回车，就能看到下载工具的界面了。

---

## 四、如何使用？

### 4.1 下载视频

1. 打开 B站网页，找到你想下载的视频
2. 复制视频链接（浏览器地址栏里 `bilibili.com/video/` 开头的那一串）
   > 也可以直接复制 BV 号（如 `BV1xx411c7mD`）、av 号（如 `av123456`）、ep 号或 ss 号，工具会自动补全为完整链接
3. 回到下载工具页面，在「添加下载链接」的大输入框里 **粘贴链接**
4. 点击粉色按钮 **「开始下载」**
5. 等待下载完成，进度条走满就说明下载好了

### 4.2 仅下载音频（MP3）

1. 粘贴链接后，打开 **「仅下载音频（提取 MP3）」** 旁边的开关（点击圆点变成粉色）
2. 点击「开始下载」
3. 下载完成后得到的是 MP3 文件，可以直接用音乐播放器听

### 4.3 批量下载

1. 在输入框里每行粘贴一个链接，像这样：

   ```
   https://www.bilibili.com/video/BV1xx411c7mD
   BV1xx411c7mE
   av123456
   ```

   > 支持完整链接、BV 号、av 号、ep 号、ss 号，工具会自动补全

2. 点击「开始下载」，会同时开始下载所有视频

### 4.4 查看下载的文件

- 点击顶部的 **「历史记录」** 标签，可以看到所有下载过的文件
- 点击每行右边的 **文件夹图标**，会自动打开文件所在的文件夹
- 默认下载位置是 `bilibili-app` 文件夹里的 `downloads` 目录

### 4.5 修改下载保存位置

1. 点击顶部 **「设置」** 标签
2. 在「下载目录」输入框里修改路径，例如改成 `D:\我的视频`
3. 点击 **「保存设置」**

---

## 五、高级功能：使用 Cookie 下载高清视频

B站对未登录用户限制视频画质。登录后可以下载 1080P 甚至更高画质。

### 5.1 获取 Cookie（用浏览器插件）

1. 打开 Chrome 或 Edge 浏览器
2. 安装插件 **「cookies.txt」**（在浏览器扩展商店搜索）
3. 安装后，打开 B站并 **登录你的账号**
4. 点击浏览器右上角的 cookies.txt 插件图标
5. 点击 **「Export」**（导出），会下载一个 cookies.txt 文件
6. 用记事本打开这个文件，**全选复制** 里面的所有内容

### 5.2 粘贴到工具里

1. 在下载工具页面，点击 **「设置」** 标签
2. 在「Cookie 配置」的输入框里 **粘贴** 刚才复制的内容
3. 点击 **「保存设置」**

> ⚠️ Cookie 相当于你的账号密码，**千万不要分享给别人**！

---

## 六、每次使用后的操作

### 关闭程序

1. 回到之前那个命令窗口（蓝/黑色窗口）
2. 按键盘 `Ctrl + C`，程序就会停止
3. 关掉窗口即可

### 下次再用

1. 打开 `bilibili-app` 文件夹
2. **双击 `start.bat`** 即可自动启动（推荐）
3. 备用方式：在空白处 `Shift + 右键` → 「在此处打开 PowerShell」，输入 `python app.py` 回车
4. 浏览器打开 `http://localhost:5001`

---

## 七、常见问题

### Q1：输入 python 命令后提示 "python 不是内部或外部命令"

说明 Python 没有装好，或者安装时没有勾选「Add Python to PATH」。
**解决方法**：重新运行 Python 安装程序，选择「Modify」（修改），确保勾选「Add Python to PATH」，然后重新安装。

### Q2：下载失败，显示错误信息

可能的原因：
- **链接不对**：确保是 B站视频/番剧链接，或有效的 BV号/av号/ep号/ss号
- **网络问题**：检查网络连接，稍后重试
- **视频需要登录**：参考第五章，配置 Cookie 后再试

### Q3：下载速度很慢

这是 B站服务器的限制，属于正常现象。可以尝试：
- 关掉其他占用网络的程序
- 避开网络高峰时段

### Q4：下载的视频没有声音

少数视频的音视频是分离的。程序会自动合并，但如果电脑没有安装 **FFmpeg**，合并会失败。
**解决方法**：下载 FFmpeg（比较进阶，需要帮助可以搜索"Windows 安装 FFmpeg 教程"）

### Q5：怎么把下载的 MP4 传到手机上？

- **微信**：文件如果小于 100MB，可以直接拖到微信聊天窗口发送
- **数据线**：用数据线连接电脑和手机，把文件复制到手机里
- **网盘**：上传到百度网盘，手机端下载

---

## 八、简单总结

| 步骤 | 操作 | 频率 |
|------|------|------|
| 1 | 安装 Python | 一辈子一次 |
| 2 | **双击 `start.bat`** | 每次使用 |

---

> 如果还有不明白的地方，可以截图发给我，我帮你看看。
